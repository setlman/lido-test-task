services:
  alertmanager:
    image: prom/alertmanager
    container_name: alertmanager
    ports:
      - "9093:9093"
    restart: always
    networks:
      - monitoring
    labels:
      prometheus_job: "alertmanager"
      prometheus_port: "9093"


networks:
  monitoring:
    external: true